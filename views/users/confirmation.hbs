{{> header}}
<div class="contenedor center">
    {{#if info}}
        <div class="text-success">
            {{info}}
        </div>
    {{else if error}}
        <div class="text-danger" id="msgError">
            * {{error}}
        </div>
    {{/if}}
</div>
